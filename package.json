{
  "name": "inventory-management-system",
  "version": "1.0.0",
  "description": "Simple full-stack inventory management system with Express, SQLite, and session-based authentication.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "db:init": "node scripts/initDb.js",
    "lint": "node --check src/server.js && find src public/js scripts -name '*.js' -print0 | xargs -0 -n1 node --check"
  },
  "keywords": [
    "inventory",
    "express",
    "sqlite",
    "session-auth"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "better-sqlite3": "^11.8.1",
    "connect-sqlite3": "^0.9.15",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-session": "^1.18.1"
  }
}
